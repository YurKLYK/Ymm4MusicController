<UserControl x:Class="BrowserMusicController.BrowserMusicControllerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="420">
    <Grid>
        <Grid.Resources>
            <Style x:Key="RoundIconButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="54"/>
                <Setter Property="Height" Value="54"/>
                <Setter Property="Foreground" Value="#FFF7F8FA"/>
                <Setter Property="Background" Value="#66FFFFFF"/>
                <Setter Property="BorderBrush" Value="#7FFFFFFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="8,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="RoundIconToggleButtonStyle" TargetType="ToggleButton">
                <Setter Property="Foreground" Value="#FFF7F8FA"/>
                <Setter Property="Background" Value="#66FFFFFF"/>
                <Setter Property="BorderBrush" Value="#7FFFFFFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid>
                                <Ellipse Name="Bg" Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="Bg" Property="Fill" Value="#22FFFFFF"/>
                                    <Setter TargetName="Bg" Property="Stroke" Value="#44FFFFFF"/>
                                    <Setter Property="Foreground" Value="#66FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MiniPillButtonStyle" TargetType="Button">
                <Setter Property="Foreground" Value="#FFE8EAED"/>
                <Setter Property="Background" Value="#3AFFFFFF"/>
                <Setter Property="BorderBrush" Value="#66FFFFFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="RoundSeekSliderStyle" TargetType="Slider">
                <Setter Property="Height" Value="18"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Slider">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="0"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="0"/>
                                </Grid.ColumnDefinitions>
                                <Track Name="PART_Track" Grid.Column="1" VerticalAlignment="Center">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="Slider.DecreaseLarge" Focusable="False" BorderThickness="0" Background="Transparent">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="6" CornerRadius="0" Background="#FFEF9D26"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="Slider.IncreaseLarge" Focusable="False" BorderThickness="0" Background="Transparent">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="6" CornerRadius="0" Background="#40FFFFFF"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Width="14" Height="14" BorderThickness="1" BorderBrush="#99FFFFFF" Background="#FFF3F5F7">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Image Name="AlbumBackgroundImage" Stretch="UniformToFill" Opacity="0.58"/>
        <Rectangle Name="BackgroundShade" Fill="#CC111318"/>
        <Canvas Name="WaveCanvas" Height="148" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="14,0,14,12" Opacity="0.9" IsHitTestVisible="False" ClipToBounds="True"/>

        <Border Name="MainCard" Margin="10" Padding="10" CornerRadius="14" BorderBrush="#66FFFFFF" BorderThickness="1" Background="#B31D212B">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="82"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Name="HeaderText" Text="Spotify / Browser Media" FontSize="10" FontWeight="SemiBold" Foreground="#D8FFFFFF"/>
                        <TextBlock Name="TrackTitleText" Text="再生中のメディアを待機中" Margin="0,3,0,0" FontSize="22" FontWeight="SemiBold" Foreground="#FFF8F9FA" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" LineStackingStrategy="BlockLineHeight" LineHeight="26" MaxHeight="52"/>
                        <TextBlock Name="ArtistText" Text="Spotify / YouTube など" Margin="0,2,0,0" FontSize="13" Foreground="#D0FFFFFF" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                    <Border Name="ThumbnailBorder" Grid.Column="1" Width="74" Height="74" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,4,0" CornerRadius="10" BorderBrush="#66FFFFFF" BorderThickness="1" Background="#332A2E37" ClipToBounds="True">
                        <Image Name="ThumbnailImage" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Name="ElapsedTimeText" Grid.Column="0" Text="0:00" FontSize="11" Foreground="#D6FFFFFF" VerticalAlignment="Center" Margin="0,0,7,0"/>
                    <Slider Grid.Column="1" Name="SeekSlider" Style="{StaticResource RoundSeekSliderStyle}" Minimum="0" Maximum="100" Value="0" PreviewMouseLeftButtonDown="SeekSlider_PreviewMouseLeftButtonDown" PreviewMouseLeftButtonUp="SeekSlider_PreviewMouseLeftButtonUp" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Name="TimeDisplay" Text="0:00" FontSize="11" Foreground="#D6FFFFFF" VerticalAlignment="Center" Margin="7,0,0,0"/>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,2,0,4">
                    <Button Name="PrevButton" Content="&#xE892;" Style="{StaticResource RoundIconButtonStyle}" Width="40" Height="40" FontSize="16" Margin="6,0" Click="PrevButton_Click"/>
                    <Button Name="PlayPauseButton" Content="&#xE768;" Style="{StaticResource RoundIconButtonStyle}" Width="52" Height="52" FontSize="19" Margin="7,0" Click="PlayPauseButton_Click"/>
                    <Button Name="NextButton" Content="&#xE893;" Style="{StaticResource RoundIconButtonStyle}" Width="40" Height="40" FontSize="16" Margin="6,0" Click="NextButton_Click"/>
                    <Button Name="StopButton" Content="&#xE71A;" Style="{StaticResource RoundIconButtonStyle}" Width="40" Height="40" FontSize="14" Margin="6,0" Click="StopButton_Click"/>
                    <ToggleButton Name="WaveToggleButton" Content="&#xE9D9;" Style="{StaticResource RoundIconToggleButtonStyle}" IsChecked="True" Width="40" Height="40" Margin="6,0" VerticalAlignment="Center" Checked="WaveToggleButton_Checked" Unchecked="WaveToggleButton_Unchecked"/>
                </StackPanel>

                <TextBlock Grid.Row="3" Name="StatusText" Text="スタンバイ中..." FontSize="10" Foreground="#A6FFFFFF" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,6,0,0" VerticalAlignment="Center">
                    <TextBlock Name="SessionLabelText" Text="Session" Foreground="#C8FFFFFF" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox Name="SessionSelector" Width="190" Height="22" FontSize="11" SelectionChanged="SessionSelector_SelectionChanged"/>
                    <Button Name="RefreshSessionsMiniButton" Content="⟳" Width="24" Height="22" FontSize="11" Margin="6,0,0,0" ToolTip="セッション再検出" Click="RefreshSessionsButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
